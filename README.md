# Switch to the backend branch
```bash
git checkout java-backend
```
# ⚠️ Configure database connection details in application.properties/yml

# Build and run the Spring Boot application
```bash
mvn clean install
mvn spring-boot:run
````
