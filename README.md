# Switch to the frontend branch
```bash
git checkout react-front
````
# Install dependencies
```bash
npm install
```
# Start the React application (usually runs on http://localhost:3000)
```bash
npm start
```
